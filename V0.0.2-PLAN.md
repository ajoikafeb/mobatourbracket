# Neosoul Community Event Platform v0.0.2
# Architecture & Implementation Plan

---

## CURRENT STATE (v0.0.1)

### Database Tables (5)
- `settings` — tournament config (singleton row)
- `teams` — registered teams with rosters
- `players` — individual players
- `matches` — bracket matches with scores
- `brackets` — bracket positions/seeding

### Pages (11 public + admin)
- Public: `/`, `/bracket`, `/schedule`, `/current-match`, `/events`
- Admin: `/admin`, `/admin/login`, `/admin/dashboard`, `/admin/tournament-generator`,
  `/admin/schedule-generator`, `/admin/bracket`, `/admin/schedule`, `/admin/current-match`,
  `/admin/export`, `/admin/settings`, `/admin/announce`

---

## TARGET STATE (v0.0.2)

### New Database Tables (6)

#### 1. `events`
```sql
CREATE TABLE events (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  title TEXT NOT NULL,
  slug TEXT NOT NULL UNIQUE,
  banner TEXT,
  thumbnail TEXT,
  description TEXT DEFAULT '',
  category TEXT NOT NULL DEFAULT 'tournament',
  -- category: tournament, giveaway, community_night, poker, quiz, watch_party, workshop, custom
  status TEXT NOT NULL DEFAULT 'draft',
  -- status: draft, registration_open, registration_closed, upcoming, running, completed, cancelled
  registration_status TEXT NOT NULL DEFAULT 'closed',
  -- registration_status: open, closed, invite_only
  start_date TIMESTAMPTZ,
  end_date TIMESTAMPTZ,
  location TEXT DEFAULT '',
  max_participants INTEGER DEFAULT 0,
  current_participants INTEGER DEFAULT 0,
  featured BOOLEAN DEFAULT false,
  published BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

#### 2. `event_categories`
```sql
CREATE TABLE event_categories (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  slug TEXT NOT NULL UNIQUE,
  icon TEXT DEFAULT 'Tag',
  color TEXT DEFAULT '#FF7A00',
  sort_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

#### 3. `registration_forms`
```sql
CREATE TABLE registration_forms (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  event_id UUID NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  title TEXT NOT NULL DEFAULT 'Registration Form',
  description TEXT DEFAULT '',
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

#### 4. `registration_fields`
```sql
CREATE TABLE registration_fields (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  form_id UUID NOT NULL REFERENCES registration_forms(id) ON DELETE CASCADE,
  field_type TEXT NOT NULL,
  -- field_type: text, long_text, email, phone, number, dropdown, radio, checkbox, textarea, date, upload, discord, telegram, kick_username, ml_id, server_id, custom
  label TEXT NOT NULL,
  placeholder TEXT DEFAULT '',
  required BOOLEAN DEFAULT false,
  options JSONB DEFAULT '[]',
  -- for dropdown/radio/checkbox: ["Option 1", "Option 2"]
  validation JSONB DEFAULT '{}',
  -- { min: number, max: number, pattern: string }
  sort_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

#### 5. `registration_responses`
```sql
CREATE TABLE registration_responses (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  event_id UUID NOT NULL REFERENCES events(id) ON DELETE CASCADE,
  form_id UUID NOT NULL REFERENCES registration_forms(id) ON DELETE CASCADE,
  respondent_name TEXT DEFAULT '',
  respondent_email TEXT DEFAULT '',
  data JSONB NOT NULL DEFAULT '{}',
  -- { field_id: value } or { field_label: value }
  status TEXT NOT NULL DEFAULT 'pending',
  -- status: pending, approved, rejected
  notes TEXT DEFAULT '',
  submitted_at TIMESTAMPTZ DEFAULT now(),
  reviewed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

#### 6. `announcements`
```sql
CREATE TABLE announcements (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  title TEXT NOT NULL,
  content TEXT NOT NULL DEFAULT '',
  type TEXT NOT NULL DEFAULT 'info',
  -- type: info, warning, success, event, update
  pinned BOOLEAN DEFAULT false,
  published BOOLEAN DEFAULT true,
  event_id UUID REFERENCES events(id) ON DELETE SET NULL,
  published_at TIMESTAMPTZ DEFAULT now(),
  expires_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

#### 7. `settings` — ADD NEW COLUMNS (no migration break)
```sql
ALTER TABLE settings ADD COLUMN IF NOT EXISTS community_name TEXT DEFAULT 'Neosoul Indonesia';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS primary_color TEXT DEFAULT '#FF7A00';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS secondary_color TEXT DEFAULT '#FFA726';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS hero_banner TEXT;
ALTER TABLE settings ADD COLUMN IF NOT EXISTS discord_url TEXT DEFAULT '';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS kick_url TEXT DEFAULT '';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS instagram_url TEXT DEFAULT '';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS website_url TEXT DEFAULT '';
ALTER TABLE settings ADD COLUMN IF NOT EXISTS version TEXT DEFAULT 'v0.0.2';
```

---

## IMPLEMENTATION PHASES

### Phase 1: Database + Types + Services
**Files:** `src/lib/types.ts`, `src/lib/supabase/`, SQL migration scripts

- [ ] Create SQL migration script for all new tables
- [ ] Add RLS policies for public read / admin write
- [ ] Enable realtime on new tables
- [ ] Add new TypeScript interfaces: `Event`, `EventCategory`, `RegistrationForm`, `RegistrationField`, `RegistrationResponse`, `Announcement`
- [ ] Extend `Settings` interface with new fields
- [ ] Create service modules:
  - `src/services/event-service.ts`
  - `src/services/registration-service.ts`
  - `src/services/announcement-service.ts`

### Phase 2: Event System — Admin
**Files:** `src/app/admin/events/`, `src/components/admin/events/`

- [ ] `/admin/events` — Event list with status filters
- [ ] `/admin/events/new` — Create event form
- [ ] `/admin/events/[id]` — Edit event form
- [ ] Event actions: publish, unpublish, duplicate, archive, delete
- [ ] Event categories management
- [ ] Admin sidebar updated with "Events" link

### Phase 3: Event System — Public
**Files:** `src/app/events/`, `src/components/public/events/`

- [ ] `/events` — Public event listing with category/status filters
- [ ] `/events/[slug]` — Event detail page (banner, description, registration CTA)
- [ ] Featured event component for homepage

### Phase 4: Registration System
**Files:** `src/app/admin/registrations/`, `src/components/admin/forms/`

- [ ] Form Builder — drag-and-drop field editor
- [ ] Field types: all 17 types specified
- [ ] `/events/[slug]/register` — Public registration form
- [ ] `/admin/registrations` — View all registrations across events
- [ ] `/admin/events/[id]/registrations` — Per-event registrations
- [ ] Actions: approve, reject, search, filter
- [ ] Export: CSV, Excel
- [ ] "Generate Tournament" from approved registrations

### Phase 5: Announcements
**Files:** `src/app/admin/announcements/`, `src/components/public/announcements/`

- [ ] `/admin/announcements` — CRUD list
- [ ] Create/edit announcement form
- [ ] Pin, schedule, archive actions
- [ ] Public announcement display component for homepage
- [ ] Announcements bar/banner component

### Phase 6: Settings Upgrade
**Files:** `src/app/admin/settings/`

- [ ] Extend existing settings page with new sections:
  - Branding (logo, colors, hero banner)
  - Community Info (name, version)
  - Social Links (Discord, Kick, Instagram, Website)
- [ ] Settings update triggers refresh on public site

### Phase 7: Homepage Dynamic Content
**Files:** `src/app/page.tsx`, `src/hooks/`

- [ ] Hero section: pull from events (featured/running)
- [ ] Announcement banner at top
- [ ] Featured event highlight
- [ ] Registration open section
- [ ] Running event section
- [ ] Upcoming events section
- [ ] Keep existing tournament stats + bracket roadmap when tournament is active
- [ ] Footer: dynamic from settings (social links, community name)

### Phase 8: Admin Dashboard Upgrade
**Files:** `src/app/admin/dashboard/`, `src/app/admin/layout.tsx`

- [ ] Updated sidebar navigation:
  - Dashboard
  - Events (NEW)
  - Registrations (NEW)
  - Announcements (NEW)
  - Tournament (existing submenu)
  - Settings
- [ ] Dashboard home: event overview, recent registrations, announcements

### Phase 9: Version Display + QA
- [ ] Display "v0.0.2" in admin dashboard footer
- [ ] Display "v0.0.2" in settings page
- [ ] Full regression test on existing tournament features
- [ ] Build + deploy

---

## NAVIGATION MAP

### Admin Sidebar (v0.0.2)
```
Dashboard
Events           ← NEW
Registrations    ← NEW
Announcements    ← NEW
─── Tournament ───
  Tournament Generator
  Schedule Generator
  Bracket Editor
  Schedule Editor
  Current Match
Export Data
Settings
```

### Public Navigation (v0.0.2)
```
Home
Events           ← NEW (or keep existing /events)
Bracket
Schedule
Current Match
```

---

## BACKWARD COMPATIBILITY GUARANTEE

1. **All existing pages remain at same URLs**
2. **All existing DB tables untouched** (only settings gets additive columns)
3. **Tournament engine untouched** — bracket, schedule, current-match all work same
4. **Existing admin pages untouched** — only new pages added
5. **Settings page extended, not replaced**
6. **Homepage extended, not replaced** — existing sections kept, new sections added

---

## FILE CREATION SUMMARY

### New Files (est. ~25)
```
src/app/admin/events/page.tsx
src/app/admin/events/new/page.tsx
src/app/admin/events/[id]/page.tsx
src/app/admin/registrations/page.tsx
src/app/admin/announcements/page.tsx
src/app/events/[slug]/page.tsx
src/app/events/[slug]/register/page.tsx
src/components/admin/event-editor.tsx
src/components/admin/event-list.tsx
src/components/admin/registration-list.tsx
src/components/admin/announcement-editor.tsx
src/components/admin/form-builder.tsx
src/components/public/event-card.tsx
src/components/public/event-featured.tsx
src/components/public/announcement-bar.tsx
src/components/public/registration-form.tsx
src/services/event-service.ts
src/services/registration-service.ts
src/services/announcement-service.ts
src/hooks/use-events.ts
src/hooks/use-announcements.ts
```

### Modified Files (est. ~10)
```
src/lib/types.ts                          — add new interfaces + extend Settings
src/app/admin/layout.tsx                  — new sidebar links
src/app/admin/dashboard/page.tsx          — add event overview
src/app/admin/settings/page.tsx           — add new settings sections
src/app/page.tsx                          — dynamic homepage content
src/components/shared/navbar.tsx          — add Events link
src/components/shared/footer.tsx          — social links from settings
```
